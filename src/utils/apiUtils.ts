// Empty placeholder file to prevent import errors
// These functions were previously used but have been removed

export const saveMapToServer = async () => {
  console.warn('saveMapToServer has been deprecated');
  return null;
};

export const getMapFromServer = async () => {
  console.warn('getMapFromServer has been deprecated');
  return null;
}; 